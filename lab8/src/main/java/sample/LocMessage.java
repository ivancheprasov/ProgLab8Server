package sample;

import java.io.Serializable;

public enum LocMessage implements Serializable {
    CONNECTED,
    INFO,
    IMPORT,
    ADD,
    REMOVE,
    LOG,
    REG,
    ADD_NOT,
    REMOVE_NOT,
    COLLECTION,
    ADD_IF_MIN_NOT,
    FOREIGN,
    LOGIN_USED,
    LOG_NOT,
    UPDATE
}
